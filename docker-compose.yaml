services:
  fooder:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ALLOWED_ORIGINS=http://localhost:3000
      - API_KEY=dupadupa
      - SQLITE_DSN=/app/fooder.db
      - SERVER_PORT=8080
    volumes:
      - ./fooder.db:/app/fooder.db
      - ./public:/app/public
      - ./api/openapi:/app/api/openapi

  # fooder-db:
  #   image: postgres:15.10-alpine3.21
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     - POSTGRES_USER=fooder
  #     - POSTGRES_PASSWORD=pwd123!
  #     - POSTGRES_HOST_AUTH_METHOD=trust
  #   volumes:
  #     - fooder-db-data:/var/lib/postgresql/data

volumes:
  fooder-db-data:
